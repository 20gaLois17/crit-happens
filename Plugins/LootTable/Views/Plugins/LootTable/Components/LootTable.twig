<h3 class="table__type" data-collapse="" data-collapse-target=".{{ raid }}--{{ type }}">{{ i18n(type) }}</h3>
<table class="loot_table {{ raid }}--{{ type }} collapsed">
    <tbody>
    {% for item in loot_table[raid][type] %}
        <tr>
            <td class="loot-item">
                <a href="https://de.classic.wowhead.com/item={{ item.itemNumber }}"></a>
            </td>
            <td class="loot-need">
                <form method="post" action="{{ url('frontend_account_loottable_preferences') }}">
                    <input type="hidden" name="itemId" value="{{ item.id }}">

                    {% if item.id in loot_preferences_users[current_user.id] %}
                        <button class="is-need" type="submit" data-tippy-content="<strong>Auswahl zurücksetzen</strong>">
                            <icon>
                                <svg class="icon icon--primary">
                                    <use xlink:href="#check"></use>
                                </svg>
                            </icon>
                        </button>
                    {% else %}
                        <button type="submit" data-tippy-content="<strong>NEEEEED!</strong>">
                            <icon>
                                <svg class="icon icon--primary">
                                    <use xlink:href="#heart"></use>
                                </svg>
                            </icon>
                        </button>
                    {% endif %}
                </form>
            </td>
            <td class="loot-player">
                <button
                    data-tippy-content="{% for user in loot_preferences_items[item.id] %}<span><strong>{{ user }}</strong></span><br>{% endfor %}">
                    <icon>
                        <svg class="icon icon--primary">
                            <use xlink:href="#insertion"></use>
                        </svg>
                    </icon>
                </button>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

